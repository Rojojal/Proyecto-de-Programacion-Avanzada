@model Libreria.Models.IndexViewModel
@{
    ViewBag.Title = "Administrar";
}

<main class="container py-5" aria-labelledby="title">
    <section class="text-center mb-4">
        <h1 id="title" class="display-4 fw-bold" style="color: #2c3e50;">@ViewBag.Title</h1>
        <p class="text-success">@ViewBag.StatusMessage</p>
    </section>

    <div class="card shadow-sm border-0 rounded">
        <div class="card-body">
            <h4 class="fw-bold mb-4">Pedidos</h4>
            <hr />

            <dl class="row">
                

                    <a href="@Url.Action("Index", "Pedido", new {})" class="btn btn-warning btn-md w-50 me-1">
                        <i class="fas fa-edit"></i> Historial de pedidos
                    </a>
                </dd>
            </dl>
        </div>
    </div>
</main>
